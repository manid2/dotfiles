dotfiles
========

`~/.dotfiles` repository to synchronize user configurations.

Install
-------

Use `install` script.

```sh
./install
```

Uninstall
---------

Use `uninstall` script.

```sh
./uninstall
```
